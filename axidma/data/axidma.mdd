OPTION psf_version = 2.1;

BEGIN driver axidma

  OPTION supported_peripherals = (axi_dma);
  OPTION supported_os_types = (DTS);
  OPTION driver_state = ACTIVE;
  OPTION NAME = axidma;  

  PARAMETER name = compatible, type = stringlist, default = "xlnx,axi-dma-1.00.a";
  PARAMETER name = axistream-connected, type = reference;
  PARAMETER name = axistream-control-connected, type = reference;

END driver
