OPTION psf_version = 2.1;

BEGIN driver qspips

  OPTION supported_peripherals = (ps7_qspi);
  OPTION supported_os_types = (DTS);
  OPTION driver_state = ACTIVE;
  OPTION NAME = qspips;
  PARAMETER name = "#address-cells", default = 1, type = int;
  PARAMETER name = "#size-cells", default = 0, type = int;
  PARAMETER name = is-dual, type = int;
  PARAMETER name = num-chip-select, default = 1, type =int;
  
END driver
